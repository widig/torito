// resultado incrivel poder compilar e receber o retorno diretamente no alert

// como estender esse resultado para algo dentro da interface?
// 1-> json
// parse do json e execução

// json tipo imagem -> abre imagem
// json tipo audio -> abre audio
// json tipo video -> abre video
// json tipo html -> renderiza html
// json tipo pdf -> abre pdf

// padronizar a lib que escreve em json para c, java, cs, python, lua, perl
// input do programa arquivo json

// padronizar requests interlang
// retorna json tipo ask -> 
//      ask peder em um url padronizada um recurso
//      salvar ponto de retorno na linguagem
//      key = interop.save();
//      interopt.ask({ url : "method", data :{ item1 : json.write(resource) } , key : key });
//      return;
//
//      interop.restore(file);
//
//
// from code to live
//

/* Hello World program */
#include<stdio.h>
int main() {
    int *d;
    int a = 10;
    int b = 100;
    d = &a;
    printf("{ \"result\" : \">> Hello World %d\" }",*d+b);
}
